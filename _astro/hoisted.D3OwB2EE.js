import{p as m}from"./planner.xpVKhZYm.js";const y=864e5,w=r=>r.toISOString().slice(0,10);document.querySelectorAll("[data-backlog-planner]").forEach(r=>{const o=r.querySelector("form"),d=r.querySelector("[data-result]"),i=r.dataset.view||"deadline",c=o.elements.namedItem("startDate"),u=o.elements.namedItem("targetDate"),h=new Date;let k=!1;const p=n=>window.cardflowTrack?.(n,i==="workflow"?"workflow":"deadline"),v=()=>{k||(k=!0,p("tool_start"))};c.value=w(h),u.value=w(new Date(h.getTime()+60*y)),o.addEventListener("input",v,{once:!0}),o.addEventListener("submit",n=>{n.preventDefault(),v();const l=t=>Number(o.elements.namedItem(t).value),g=[...r.querySelectorAll("[data-stage]")].flatMap(t=>t.querySelector("[name=enabled]").checked?[{label:t.querySelector("[name=label]").value,scopePercent:Number(t.querySelector("[name=scopePercent]").value),cardsPerHour:Number(t.querySelector("[name=cardsPerHour]").value),workers:Number(t.querySelector("[name=workers]").value)}]:[]),e=m({totalCards:l("totalCards"),hoursPerWorkday:l("hoursPerWorkday"),workdaysPerWeek:l("workdaysPerWeek"),startDate:c.value,targetDate:u.value,stages:g});if(e.status==="INPUT_REQUIRED"){d.innerHTML="<h3>More input is needed</h3><p>Check the dates, active stages, rates, percentages and worker counts.</p>";return}p("tool_complete");const b=e.feasible?"Fits the target window":"Capacity shortfall",s=t=>t.replace(/[<>&]/g,"");if(i==="workflow"){const t=e.stages.map((a,f)=>`<tr><td>${f+1}</td><td>${s(a.label)}</td><td>${a.cardsHandled.toLocaleString()}</td><td>${a.workHours}</td><td>${a.workflowSharePercent}%</td><td>${a.cumulativeWorkdays}</td></tr>`).join("");d.innerHTML=`<div class="result-top ${e.feasible?"ok":"short"}"><div><span>Selected stages</span><strong>${e.stages.length}</strong></div><div><span>Card-stage operations</span><strong>${e.totalStageCardTouches.toLocaleString()}</strong></div><div><span>Sequential workdays</span><strong>${e.totalWorkdays}</strong></div><div><span>Slowest stage</span><strong>${s(e.bottleneck.label)}</strong></div></div><p>The selected sequence requires <b>${e.totalWorkHours} workflow hours</b>. Across this date window that is ${e.requiredWorkflowHoursPerAvailableWorkday} workflow hours and ${e.requiredStageCardTouchesPerWorkday.toLocaleString()} card-stage operations per available workday. This is a sequential handoff map; it does not assume simultaneous stages or invent queue arrival rates.</p><div class="table-wrap"><table><thead><tr><th>Step</th><th>Stage</th><th>Assigned cards</th><th>Hours</th><th>Share</th><th>Cumulative workday</th></tr></thead><tbody>${t}</tbody></table></div>`}else{const t=e.stages.map(a=>`<tr><td>${s(a.label)}</td><td>${a.cardsHandled.toLocaleString()}</td><td>${a.effectiveCardsPerHour.toLocaleString()}</td><td>${a.workHours}</td><td>${a.workdays}</td></tr>`).join("");d.innerHTML=`<div class="result-top ${e.feasible?"ok":"short"}"><div><span>Status</span><strong>${b}</strong></div><div><span>Projected finish</span><strong>${e.projectedCompletionDate}</strong></div><div><span>Latest start</span><strong>${e.latestStartDate}</strong></div><div><span>Workday slack</span><strong>${e.workdaySlack}</strong></div></div><p><b>Bottleneck:</b> ${s(e.bottleneck.label)} (${e.bottleneck.workdays} workdays). Required average across the target window: ${e.requiredWorkflowHoursPerAvailableWorkday} workflow hours and ${e.requiredStageCardTouchesPerWorkday.toLocaleString()} card-stage operations per available workday. Shortfall: ${e.capacityShortfallWorkHours} workflow hours.</p><div class="table-wrap"><table><thead><tr><th>Stage</th><th>Cards</th><th>Effective / hour</th><th>Hours</th><th>Workdays</th></tr></thead><tbody>${t}</tbody></table></div>`}})});
